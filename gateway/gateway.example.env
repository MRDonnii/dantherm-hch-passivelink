# Use the stable path reported by: ls -l /dev/serial/by-id/
RS485_DEVICE=/dev/serial/by-id/REPLACE_WITH_YOUR_ADAPTER

# Listen on all local interfaces. Restrict access with a firewall if enabled.
GATEWAY_BIND=0.0.0.0
GATEWAY_PORT=4196
